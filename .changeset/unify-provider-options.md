---
"mini-agents": patch
---

Unify `anthropicOptions`/`openaiOptions`/`openaiChatOptions` into a single `providerOptions` field in `LLMClientOptions`. Add OpenAI Chat Completions API client (`openai-chat` provider).
