---
"mini-agents": patch
---

refactor: modularize Agent class by extracting responsibilities into separate files (summarizer, cancellation, tool-executor, types) while keeping the public API unchanged.
